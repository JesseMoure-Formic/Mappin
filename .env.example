# Web is served same-origin, so it uses a relative "/api" base and needs nothing here.
# Native (iOS/Android) builds must point at an absolute API URL ending in /api:
EXPO_PUBLIC_API_URL=http://localhost:3000/api
